#include<bits/stdc++.h>
using namespace std;
int main(){
       float a;
       cin>>a;
       int k;
       cin>>k;
       int y = pow(2,k);
       cout<<fixed;
       cout.precision(3);
       cout<<pow(a,y);

    return 0;
}